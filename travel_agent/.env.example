# Copy this file to .env and replace the placeholder values with your credentials.
# Then export them into your environment before running the travel agent, e.g.:
#   export $(grep -v '^#' .env | xargs)
# Alternatively, load them with your preferred secrets manager tooling.

# Required: Amadeus API client identifier
AMADEUS_CLIENT_ID=replace-with-your-client-id

# Required: Amadeus API client secret
AMADEUS_CLIENT_SECRET=replace-with-your-client-secret

# Optional overrides:
#AMADEUS_HOSTNAME=https://test.api.amadeus.com
#AMADEUS_TIMEOUT=10.0
